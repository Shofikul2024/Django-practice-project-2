CREATE YOUR FIRST DJANGO PROJECT (Line by Line Bangla Explain)
01. Project Folder তৈরি
mkdir main_project


👉 mkdir মানে নতুন ফোল্ডার তৈরি
👉 main_project হলো আপনার Django প্রজেক্টের মূল ফোল্ডার

cd main_project


👉 cd মানে ফোল্ডারের ভেতরে প্রবেশ করা
👉 এখন আমরা main_project ফোল্ডারের ভিতরে আছি

কাজ: সব Django ফাইল এই ফোল্ডারের ভিতরে থাকবে

02. Virtual Environment তৈরি
python -m venv venv


👉 python = Python চালু করা
👉 -m = Python এর module ব্যবহার করা
👉 venv = virtual environment বানানোর module
👉 শেষের venv = environment এর নাম

গুরুত্বপূর্ণ:

❌ python-m venv ভুল (স্পেস লাগবে)

❌ python3-m venv ভুল

✔️ সঠিক: python -m venv venv

কাজ: আলাদা পরিবেশ তৈরি, যাতে অন্য প্রজেক্টে সমস্যা না হয়

03. Virtual Environment চালু করা
Windows
venv\Scripts\activate


👉 Windows এ venv চালু করার কমান্ড

Linux / macOS
source venv/bin/activate


👉 Linux / macOS এ venv চালু করার কমান্ড

নোট:
👉 Terminal এর শুরুতে (venv) দেখা মানে environment active

04. Django Install
pip install django


👉 pip = Python package install করার tool
👉 django = Django framework

গুরুত্বপূর্ণ:

venv active থাকলে pip3 দরকার নেই

Django শুধু এই প্রজেক্টেই install হবে

05. Django Project তৈরি
django-admin startproject config .


👉 django-admin = Django এর command tool
👉 startproject = নতুন প্রজেক্ট তৈরি
👉 config = project এর নাম
👉 . = বর্তমান ফোল্ডারের ভিতরে প্রজেক্ট বানাবে

নোট:
. না দিলে আলাদা folder তৈরি হবে

06. Server চালু করা
python manage.py runserver


👉 Django এর development server চালু হয়
👉 লোকাল কম্পিউটারে ওয়েবসাইট দেখা যায়

Browser এ খুলুন

👉 http://127.0.0.1:8000/
👉 এটি লোকালহোস্ট ঠিকানা (নিজের কম্পিউটার)

✅ Final Result

✔️ Django project সফলভাবে তৈরি
✔️ Server ঠিকভাবে চলছে
✔️ Django welcome page দেখা যাবে